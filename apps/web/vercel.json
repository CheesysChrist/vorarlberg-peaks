{
  "buildCommand": "next build --webpack",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile --ignore-scripts && pnpm --filter @vorarlberg-peaks/types build",
  "framework": "nextjs"
}
