{
  "buildCommand": "next build --webpack",
  "installCommand": "cd ../.. && rm -rf node_modules && pnpm install --frozen-lockfile --ignore-scripts && pnpm --filter @vorarlberg-peaks/types build",
  "framework": "nextjs"
}
