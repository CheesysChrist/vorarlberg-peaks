{
  "name": "vorarlberg-peaks",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@11.2.2",
  "engines": {
    "node": ">=22",
    "pnpm": ">=11"
  },
  "scripts": {
    "dev": "pnpm --parallel --filter './apps/*' dev",
    "build": "pnpm --filter @vorarlberg-peaks/types build && pnpm --filter api exec prisma generate && pnpm --filter api build",
    "start": "pnpm --filter api exec prisma migrate deploy && node apps/api/dist/main",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
