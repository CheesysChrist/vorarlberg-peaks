{
  "buildCommand": "pnpm --filter @vorarlberg-peaks/types build && pnpm --filter web build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs"
}
